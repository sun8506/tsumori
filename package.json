{
  "name": "tsumori",
  "version": "1.0.0",
  "private": true,
  "description": "Japanese learning and daily study tracking application",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server-db.js && node --check js/auth.js && node --check js/storage.js && node --check js/api.js && node --check js/nhk.js && node --check js/views/article.js"
  }
}
